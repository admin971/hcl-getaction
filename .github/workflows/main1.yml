name: git-action-test

on: push

jobs:
 code-checkout:
  runs-on: Ubuntu-latest
  steps:
   - name: code checkout
     uses: actions/checkout@v4.2.2
   - name: print
     run: echo ' push request work flow triggered'
